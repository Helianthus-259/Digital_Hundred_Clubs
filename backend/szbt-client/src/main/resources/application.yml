#服务端口，随便起
server:
  port: 8080
#服务名称
spring:
  application:
    name: szbt-client

#eureka地址
eureka:
  client:
    register-with-eureka: false #不向 eureka 注册自己
    service-url:
      defaultZone: http://127.0.0.1:11011/eureka/

